jQuery Auto Height
=====================

Set all matching elements to the same height.

## Usage
```
$('.sameheight').autoHeight();
```
```
$('.sameheight').autoHeight({
    minWidth: 400,
    maxWidth: 800
});
```
